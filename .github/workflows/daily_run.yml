name: Daily YouTube AI Filter Run

on:
  schedule:
    - cron: '30 15 * * *'
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser youtube-transcript-api google-genai requests

      - name: Run the script
        env:
          GEMINI_API_KEY: "AIzaSyAAlzAYaKs9099sjZKdC-1hz0Qe9K_OXQo"  # Replace with actual
          TELEGRAM_BOT_TOKEN: "8160440202:AAHRUibp3NUAfv8bwC6POVsW85i21r3mL04"  # Replace with actual
          TELEGRAM_CHAT_ID: "8511286822"  # Replace with actual
        run: |
          python daily_filter.py
